'use strict';

const { BrowserWindow } = require('electron');

const windowOptions = {
	width: 1080,
	minWidth: 680,
	height: 840,
	title: app.getName(),
	frame: false
}

module.exports = function MainWindow() {

};